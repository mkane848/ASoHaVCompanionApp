// Injected by vite.config.ts's `define`, from apps/web/package.json and the repo's CHANGELOG.md.
// __APP_RELEASE_DATE__ is a full UTC timestamp ("YYYY-MM-DDTHH:MM:SSZ") for 0.4.0 and later,
// date-only ("YYYY-MM-DD") for entries before that — see AboutModal.tsx for how each is rendered.
declare const __APP_VERSION__: string;
declare const __APP_RELEASE_DATE__: string | null;
