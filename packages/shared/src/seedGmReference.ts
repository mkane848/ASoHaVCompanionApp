import type { GmReferenceSection } from './types.js';

/** The ruleset's GM chapter as reference sections (revised V0.6 slice 8): "Running the Game", "GM
 *  Principles", "GM Moves" and "Soft & Hard Moves", verbatim. Its own file because it is long
 *  prose that nothing else in `seedLibrary.ts` needs to scroll past. See `GmReferenceSection` for
 *  the `Body` format. Filled by WP 8B. */
export const GM_REFERENCE: GmReferenceSection[] = [];
