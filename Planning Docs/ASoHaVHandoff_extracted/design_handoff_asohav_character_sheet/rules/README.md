# Moved

The six rules files that lived here were byte-duplicates of the ones in `Planning Docs/`,
differing only by a four-line attribution block — so every rules edit had two homes and one
always went stale.

Both copies are now archived together under `Planning Docs/archive/`:

- `Planning Docs/archive/` — the top-level originals
- `Planning Docs/archive/handoff-rules/` — the copies that were here

All six are **superseded**. The current ruleset is `Planning Docs/Ruleset-V0.5.md`.
